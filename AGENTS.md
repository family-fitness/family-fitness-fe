<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

---

# 우리가족 체력키움 — 프론트엔드 작업 규칙

> 사람과 AI 개발 도구가 함께 읽는 파일이다. 새 화면·컴포넌트를 만들기 전에 먼저 읽는다.
> 규칙이 바뀌면 코드가 아니라 이 파일을 먼저 고친다.

## 이 프로젝트가 뭔가

국민체력100 공개데이터 기반 가족 체력 서비스. 한국스포츠정책과학원 공공데이터 활용 경진대회 출품작.
**앱스토어에 올리지 않는 웹(PWA)** 이다. 링크로 바로 들어오고, 홈 화면에 설치되면 앱처럼 보인다.

레포가 셋으로 나뉘어 있다.

| 레포                | 내용                                                                                |
| ------------------- | ----------------------------------------------------------------------------------- |
| `family-fitness-fe` | **여기.** Next.js 프론트엔드                                                        |
| `family-fitness-be` | Kotlin + Spring Boot. 기획 문서 `docs/user-flow.md`, `docs/erd.md` 원본이 여기 있다 |
| `family-fitness-ai` | Python AI 서비스 (RAG 검색·임베딩)                                                  |

## 누구를 위한 앱인가

기획서의 페르소나 셋이 이 앱의 모든 판단 기준이다.

| 누구                    | 상황                                            | 이 앱에 바라는 것                        |
| ----------------------- | ----------------------------------------------- | ---------------------------------------- |
| **초등학교 4학년 서준** | 학원 셋, 남는 시간은 스마트폰. 줄넘기 하위권    | 게임처럼 재미있고 부모가 같이 해 주는 것 |
| **워킹맘 은영(39)**     | 걱정은 되지만 퇴근 후 지쳐 같이 못 움직임       | 죄책감 주는 앱 말고, 주말 30분부터       |
| **아빠 도현(42)**       | 건강검진 경고. 아이와 함께면 움직일 명분이 생김 | 내가 또래 대비 어디인지 눈으로 보고 싶음 |

여기서 나오는 규칙 셋.

1. **통제하지 않는다.** 스마트폰 잠금 같은 기능은 만들지 않는다
2. **죄책감을 주지 않는다.** "당신이 참여하지 않았습니다" 는 은영이 앱을 지우는 가장 빠른 길이다
3. **아이 화면은 놀이여야 한다.** 서준에게 백분위 표를 보여주면 그걸로 끝이다

서비스는 **보여주기 → 설득하기 → 실행하기 → 지속하기** 순서로 짜여 있다.
화면을 새로 만들 때 이 중 어디에 속하는지 먼저 정한다.

## 화면 구조

**하단 탭바를 쓰지 않는다.** 부모와 아이가 같은 바를 쓸 수 없고, 다섯 칸짜리 탭바는
어느 앱에나 있는 모양이라 이 서비스가 무엇인지 말해 주지 않는다.

대신 **역할이 곧 내비게이션**이다. 로그인하면 부모인지 아이인지 고르고, 그 뒤로는
자기 홈 하나가 중심이 된다. 나머지는 홈에서 밀어 올린다.

```
/                    스플래시 — 토큰을 보고 갈 곳을 정한다
/login               로그인
/start               부모 · 아이 고르기          ★ 여기서 갈린다
/start/parent        부모 정보
/start/child         아이 신체정보 (이름 · 나이 · 키 · 몸무게)

/parent              부모 홈 — 아이 신체 점수, 또래 비교, 오늘 한 일, 도장 찍기
/parent/child/[id]   아이 한 명 자세히 — 점수 추이, 키 · 몸무게 기록
/parent/update/[id]  신체정보 다시 재기

/kid                 아이 홈 — 내 점수, 오늘의 운동
/kid/play/[id]       운동하기 — 유튜브 재생 + 타이머 + 완료
/kid/stamps          받은 도장

/settings            설정 — 역할 바꾸기, 동의, 로그아웃
```

**한 화면에 한 가지 일만 시킨다.** 특히 아이 화면은 누를 것이 둘을 넘지 않는다.

## 폴더 구조

```
src/
  app/              라우트만. 화면 조립과 데이터 로딩까지
  components/
    ui/             도메인을 모르는 기본 부품 (Button, Card, Sheet)
    app-shell/      하단 탭바, 헤더 등 앱 껍데기
    domain/         이 서비스를 아는 컴포넌트 (MemberCard, GradeBadge)
  lib/
    api/            백엔드 계약 — types.ts, client.ts, queries.ts
    *.ts            순수 함수. 여기 있는 건 전부 테스트 가능해야 한다
  mocks/            MSW. 백엔드가 없는 동안의 가짜 서버
  stores/           Zustand. 서버가 모르는 상태만
  providers/        Provider 컴포넌트
```

`components/ui` 와 `components/domain` 을 나누는 기준은 **이 서비스를 아느냐**다.
`Button` 은 다른 프로젝트에 그대로 옮겨도 되지만 `GradeBadge` 는 안 된다.

## 상태를 어디에 둘지

| 종류               | 어디에         | 예                                      |
| ------------------ | -------------- | --------------------------------------- |
| 서버에서 온 것     | TanStack Query | 가족 체력 지도, 측정 결과, 미션 목록    |
| URL 로 표현되는 것 | searchParams   | 선택된 탭, 필터                         |
| 그 외 전역         | Zustand        | 지금 보고 있는 프로필, 진행 중인 타이머 |

서버에서 받은 데이터를 `useState` 나 Zustand 에 복사해두지 않는다. 두 개가 어긋나기 시작한다.
"이 값이 서버에도 있나?" 를 먼저 묻고, 있으면 Query 에 맡긴다.

## 서버 컴포넌트와 클라이언트 컴포넌트

이 앱은 로그인 뒤에서만 돌고 검색 노출이 필요 없다. 대부분 클라이언트 컴포넌트다.

`"use client"` 는 **필요한 가장 아래 컴포넌트에** 붙인다. 페이지 최상단에 붙이면
그 아래 전부가 클라이언트 번들로 딸려 내려간다.

## 타입

- `any` 금지. ESLint 가 막는다. 정말 필요하면 `eslint-disable` 주석에 이유를 쓴다
- 백엔드 응답 타입은 `src/lib/api/types.ts` 한 곳에서만 정의한다. 화면에서 다시 만들지 않는다
- 실제 API 가 올라오면 손으로 쓴 타입을 버리고 OpenAPI 문서에서 자동 생성으로 갈아탄다

```bash
npx openapi-typescript http://localhost:8080/v3/api-docs -o src/lib/api/schema.ts
```

## 스타일

- Tailwind. 디자인 토큰은 `src/app/globals.css` 의 `@theme` 에 둔다
- 색은 토큰으로만 쓴다. `text-[#3b82f6]` 같은 임의값 금지
- 모바일 우선. 데스크톱은 가운데 정렬된 폰 너비 컨테이너로 본다
- 안전 영역(노치·홈 인디케이터)은 `env(safe-area-inset-*)` 로 처리한다

## UI 에서 하지 말 것

- **이모지를 UI 에 쓰지 않는다.** 아이콘은 `lucide-react` 를 쓴다
- **제목·라벨 앞에 세로 막대(accent bar) 를 붙이지 않는다**
- 로딩 상태를 스피너 하나로 때우지 않는다. 실제 레이아웃 모양의 스켈레톤을 쓴다.
  기다림은 세 군데에서 따로 생긴다 — 셋 다 그려야 한다
  | 언제                | 무엇으로                             |
  | ------------------- | ------------------------------------ |
  | 화면이 넘어갈 때    | `loading.tsx` (`RouteLoading`)       |
  | 데이터를 기다릴 때  | 그 화면의 스켈레톤                   |
  | 바깥 것을 기다릴 때 | 유튜브처럼 남의 스크립트를 받는 동안 |
- 에러를 `alert()` 로 띄우지 않는다
- **하단 탭바를 만들지 않는다.** 역할이 내비게이션이다 (위 "화면 구조")

## 절대 어기면 안 되는 도메인 규칙

발표 자리에서 무너지는 지점들이다. 화면 문구 하나까지 여기에 걸린다.
`family-fitness-be` 의 `docs/user-flow.md` 에서 뽑았다.

### 1. 코치 제안은 미션이 아니다

주간 코치가 만든 것은 **제안(proposal)** 이고, 보호자가 승인해야 비로소 **미션(mission)** 이 된다.
승인 전에는 미션이 0건이다. 이건 백엔드 테스트로 증명하는 우리 서비스의 핵심 주장이다.

- 승인 전 화면에서 "미션"이라는 단어를 쓰지 않는다. "제안", "이번 주 제안"이라고 쓴다
- 승인 버튼은 `role === "PARENT"` 인 프로필에만 보인다. 자녀 계정은 버튼 자체가 없다
- 거절도 1급 동작이다. 거절 사유를 받는다

### 2. 걸음수는 자기 신고다. 자동 실측인 척하지 않는다

웹에서 서버가 진짜로 아는 건 **유튜브 재생 진행률**과 **앱 내 타이머** 둘뿐이다.

| `verifiedBy`     | 화면 표기                      | 성격           |
| ---------------- | ------------------------------ | -------------- |
| `VIDEO_PROGRESS` | "영상 완주로 확인됨"           | 서버가 안다    |
| `TIMER`          | "타이머로 확인됨"              | 서버가 안다    |
| `SELF_REPORT`    | "직접 입력함 · 부모 확인 필요" | 사람이 적은 값 |

`SELF_REPORT` 에 "자동 인증", "자동 동기화" 같은 말을 붙이지 않는다.

### 3. 10년 후 예측은 예언이 아니다

국민체력100은 횡단면 조사라 개인 추적이 불가능하다.

- 쓸 문구: "지금과 같은 조건의 10년 위 연령대는 여기 있습니다"
- 쓰면 안 되는 문구: "당신은 10년 뒤 이렇게 됩니다"
- `disclaimer` 는 화면에서 **제거하거나 접을 수 없다.** 항상 보이게 둔다
- p10~p90 신뢰구간 음영을 반드시 같이 그린다. 중앙값만 그리면 확정된 미래처럼 보인다

### 4. 측정할 수 없는 사람이 있다

- `profile.measurable === false` (만 4세 미만) 면 측정 버튼을 **렌더링하지 않는다.** 비활성화가 아니라 없앤다
- 측정 기록이 없으면 `headline === null` → "첫 측정을 등록하면 지도가 그려져요"
- 동의가 철회되면 측정·활동 저장이 403 이다. 에러 코드를 사람 말로 옮긴다

### 5. 측정 폼은 두 구역으로 나눈다

`inputGroup` 이 기준이다.

- `EASY` → "집에서 잴 수 있어요"
- `EQUIPMENT` → "장비가 있으면 더 정확해요". 악력계·넓은 공간이 필요한 항목

악력계가 있는 집이 거의 없다. 첫 화면에서 장비를 요구하면 거기서 이탈한다.
장비 구역은 접힌 상태로 시작한다.

**항목 목록은 프론트가 갖지 않는다.** 연령대마다 다르므로 매번 서버에서 받는다
(`GET /fitness/items?ageGroup=`). 목록을 하드코딩하면 유아기 화면에 성인 항목이 뜬다.

항목의 식별자는 이름이 아니라 **3자리 코드**(`"012"`)다. 화면 이름은 `itemLabel` 을
쓴다 — 왕복오래달리기는 연령대마다 거리가 달라 라벨이 따로 온다.
값 범위는 항목마다 `range` 로 온다. 보내기 전에 적는 자리에서 검사한다.

### 6. AI 코치 답변에는 근거가 붙는다

`role === "ASSISTANT"` 인데 `citations` 가 비어 있으면 **버그로 본다.**
답변 아래 인용 카드를 항상 렌더링한다.

### 7. 계정과 프로필은 다르다

로그인은 **계정(user)** 이 하고, 측정·미션·활동은 전부 **프로필(profile)** 에 달린다.
한 계정이 여러 프로필을 관리한다(부모 계정 하나로 온 가족). 화면에서 "나"를 말할 때
그게 계정인지 프로필인지 항상 구분한다.

프로필 목록을 주는 곳이 둘인데 **내용이 다르다.**

| 어디서                        | 무엇                      | 쓰는 곳                        |
| ----------------------------- | ------------------------- | ------------------------------ |
| `GET /me` 의 `profiles`       | 이 계정이 관리하는 프로필 | 프로필 전환, "내가 될 수 있는" |
| `GET /families/{id}/profiles` | 가족 구성원 전부          | 이름 찾기, 참여자 표시         |

자녀가 자기 계정을 가지면 부모의 `/me` 에서 빠진다. 가족 전체의 이름을 `/me` 에서
찾으면 자녀 이름이 조용히 "가족" 으로 떨어진다.

### 8. 등급을 경고색으로 칠하지 않는다

이 서비스는 부모가 아이를 평가하는 도구가 아니다. 낮은 등급을 빨강으로 그리면
아이가 자기 화면에서 자기가 나쁘다는 신호를 본다.

등급은 같은 색상의 명도 단계로만 구분한다.

**서버가 주는 등급은 `1등급`·`2등급`·`3등급`·`참가` 뿐이다.** 4·5등급은 없고,
규준에 못 미쳐도 「참가」다. 「미달」·「하위」·「부족」 같은 말을 프론트에서 만들어
붙이지 않는다.

등급(`grade`)이 서열이라면 **`band` 는 상태**다. 자녀 화면에서는 등급 대신 band 를
쓴다. 코드값(`strength`)을 그대로 쓰지 않고 `BAND_COPY` 를 거친다.

만 7~10세는 국민체력100 규준이 비어 있는 항목이 있어 `percentile` 과 `grade` 가
`null` 로 온다. 0% 막대를 그리면 꼴찌처럼 보인다 — 비어 있음을 비어 있게 그린다.

### 9. 서버가 준 문구를 고쳐 쓰지 않는다

`headline`, `topPercentText`, `disclaimer`, `notice` 는 **그대로 내보낸다.**
백분위에서 "상위 N%" 를 프론트가 다시 계산하면 반올림 기준이 서버와 달라져
두 화면이 서로 다른 말을 한다.

오류도 마찬가지다. 응답의 `error.message` 는 개발자용이라 화면에 그대로 쓰지 않고,
`error.code` 로 갈라 우리 문구를 낸다.

### 10. 신체 점수는 우리가 지어낸 수가 아니다

아이에게 보여주는 "신체 점수" 는 서버가 준 **또래 백분위**(`overallPercentile`)다.
프론트에서 항목을 가중평균하거나 100점 만점으로 환산하지 않는다 — 그러면 같은 아이가
부모 화면과 아이 화면에서 다른 점수를 받는다.

- 점수 옆에는 **항상 또래 평균(50) 기준선**을 같이 그린다. 기준이 없으면 62가 좋은 건지 모른다
- 측정 기록이 없으면 점수를 0으로 그리지 않는다. "아직 재지 않았어요" 다
- 아이 화면에서 점수를 **등수나 순위로 바꾸지 않는다.** "반에서 몇 등" 은 이 앱이 하는 말이 아니다

▲ 기획서의 확장 기능 "AI 체력 나이" 가 올라오면 점수 자리를 체력 나이로 바꾼다.
백엔드에 요청해 뒀다.

#### 아이 화면에서 보여주는 것과 아닌 것

| 보여준다                    | 보여주지 않는다                  |
| --------------------------- | -------------------------------- |
| 신체 점수(또래 백분위) 하나 | 등급(1·2·3·참가)                 |
| 이번 주에 움직인 날         | 약한 항목 — "너는 유연성이 약해" |
| 오늘 할 운동 하나           | 형제·가족 구성원 비교            |
| 받은 도장과 받은 말         | 연속 기록이 끊겼다는 말          |

점수 하나는 아이에게 보여준다. 자기가 어디쯤인지 아는 건 아이 권리다.
**서열로 바꾸는 것**이 문제다 — 등급, 등수, 누구보다 낫다는 말.

### 11. 아이의 몸은 자란다

키와 몸무게는 6개월이면 달라진다. 지난 기록으로 계속 비교하면 점수가 틀어진다.

- 마지막 측정이 **90일을 넘으면** 홈에서 다시 재자고 말한다. 막지는 않는다
- 다시 재기는 **덮어쓰기가 아니라 추가**다. 지난 기록이 남아야 자란 걸 보여줄 수 있다
- 재자고 말할 때 "오래됐어요" 라고 쓰지 않는다. 부모를 탓하는 말이다

### 12. 칭찬은 부모가 보낸다

아이가 운동을 마치면 부모에게 알림이 가고, **부모가** 한마디를 보낸다.
아이가 자기에게 칭찬할 수 없다. 그래야 받은 말에 값이 생긴다.

- 아이 화면의 완료 버튼은 "다 했어요" 이고, 그다음은 "엄마한테 알리기" 다
- 부모가 아직 안 보냈으면 아이 화면은 "기다리는 중" 이다. 재촉하지 않는다
- **정해진 문구를 고르기만 해도 보내진다.** 퇴근하고 지친 부모에게 글쓰기를
  시키면 그날로 안 보낸다 — 빈 칸만 있는 화면은 닫힌다
- 받은 말은 개수를 목표로 만들지 않는다. "10개 모으면" 을 붙이면 못 채운 날이
  실패가 된다

## 디자인 규칙

### 색

색은 **국민체력100 공식 사이트에서 가져온다.** 공공데이터 기반 서비스라 그 기관이 쓰는 색을
쓰는 게 맞고, AI 가 기본값으로 뱉는 색을 피하는 방법이기도 하다.

| 쓰임             | 값                                                 |
| ---------------- | -------------------------------------------------- |
| 배경             | `#FFFFFF` 흰색으로 통일. 구획이 필요하면 `#F4F4F4` |
| 포인트           | `#2784E6` (국민체력100 로그인 버튼)                |
| 진한 포인트 · 선 | `#1B2574` (로고 남색)                              |
| 기록 강조        | `#FFB800` (마스코트 노랑)                          |

**쓰지 않는 것** — 그라데이션, 네온, 보라 계열, 채도 높은 형광색.
특히 보라에서 파랑으로 흐르는 그라데이션은 AI 가 만든 화면의 대표 신호다.

**다크 모드를 만들지 않는다.** 흰 화면 하나로 간다.

기기가 다크 모드여도 앱은 흰색이다. `html` 에 `color-scheme: light` 를 못박아
브라우저가 입력칸·스크롤바·달력을 제멋대로 어둡게 칠하지 못하게 한다 —
이걸 빠뜨리면 화면은 흰데 입력칸만 검은 어중간한 상태가 된다.

**검정으로 면을 채우지 않는다.** 고른 칩, 버튼, 배지 전부 파랑(`--color-signal`)
이나 남색(`--color-signal-deep`)을 쓴다. 검정을 채우기 시작하면 공공 서비스가
아니라 흑백 앱처럼 보인다.

### AI 가 만든 티가 나는 것 — 피할 목록

조사해서 정리했다. 하나라도 하고 있으면 다시 본다.

- **균일한 라운드 카드를 나열하는 것.** 가장 흔한 신호다. 구획은 선으로 나눈다
- 모든 요소에 같은 패딩 · 같은 모서리 · 같은 크기
- 보라~파랑 그라데이션
- Inter 나 시스템 기본 폰트
- 시각적 계층 없이 평평한 화면
- 모든 요소에 똑같은 페이드인 애니메이션
- 그림 없이 글자만으로 채우는 것
- "함께 만들어가는", "당신을 위한" 같은 무난한 문구

### 움직임

**한 화면에 움직이는 것은 하나다.** 움직임은 "여기를 보라" 는 신호라서,
둘이 되면 눈이 어디를 봐야 할지 모르고 다섯이면 그냥 산만하다.

캐릭터는 **기본이 멈춤**이다(`KidCharacter` 의 `animate` 는 기본 false).
켜는 곳은 셋뿐이다.

| 켜는 곳                | 왜                    |
| ---------------------- | --------------------- |
| 지금 눌러야 할 것      | 오늘 할 운동 카드     |
| 지금 따라 해야 할 동작 | 놀이 중 캐릭터        |
| 방금 해낸 순간         | 다 했어요 · 칭찬 받기 |

멈춰 있을 때는 **그 동작이 가장 잘 읽히는 프레임**을 보여준다 — 점프는 공중,
앉기는 가장 낮은 자세. 1번 프레임은 대개 그냥 서 있는 모습이라 밋밋하다.

#### 캐릭터 말고 움직여야 하는 것들

움직임은 캐릭터만의 일이 아니다. 오히려 아래가 "앱 같다" 는 느낌을 만든다.

| 무엇         | 어떻게                                                          |
| ------------ | --------------------------------------------------------------- |
| 화면 전환    | 앞으로 갈 땐 오른쪽에서, 뒤로 갈 땐 왼쪽에서 (`ViewTransition`) |
| 위 막대      | 전환 중 **제자리에 머문다.** 같이 움직이면 기준점이 사라진다    |
| 점수 링·숫자 | 0에서 같이 차오른다. 숫자만 툭 뜨면 둘이 따로 논다              |
| 기록 막대    | 왼쪽에서 자란다                                                 |
| 시트         | 아래에서 올라온다                                               |
| 누름         | 살짝 눌린다 (`.press`)                                          |
| 완료 · 칭찬  | 조각이 한 번 터진다 (three.js)                                  |

#### 전환에서 지킬 것

- 타입이 없는 이동(브라우저 뒤로가기, 새로고침)은 **아무 일도 일어나지 않는다.**
  기기가 하는 동작과 겹치면 두 번 움직이는 것처럼 보인다
- 전환 중에도 누를 수 있어야 한다 (`::view-transition { pointer-events: none }`).
  덮개가 클릭을 삼키면 빨리 누르는 아이가 "안 눌려" 라고 느낀다
- `prefers-reduced-motion` 이면 전부 0초
- **`fade` 같은 키프레임을 반드시 정의한다.** 이름만 쓰고 정의가 없으면 그 선언은
  통째로 무시되고, 옛 화면이 투명해지지 않은 채 겹쳐 미끄러져 **잔상이 남는다**
- 겹치는 동안 `mix-blend-mode: normal` 로 둔다. 브라우저 기본값(plus-lighter)은
  기본 페이드와 맞물릴 때만 맞는 값이라 직접 애니메이션을 주면 색이 들뜬다

### three.js 는 이렇게만 쓴다

3D 모델을 만들지 않는다. 에셋이 플랫 2D 벡터라 입체를 섞으면 두 개의 다른 앱처럼 보인다.

**쓰는 곳** — 조각 수십~수백 개가 동시에 움직이는 순간뿐이다.

1. 누를 때마다 튀는 조각 (놀이). 1분에 40번 × 8조각이면 320개다
2. 완료 · 칭찬 축하 컨페티

**쓰지 않는 곳**

- **배경에 조각을 띄우는 것.** 흰 배경에 구름·별이 떠다니면 이모지를 뿌려 놓은
  것처럼 보인다. 배경은 **가로로 긴 장면 한 장**(`bg/`)을 깐다
- 그라데이션 셰이더, 3D 카드 틸트, 파티클 도배.
  전부 AI 가 만든 화려한 랜딩에서 가장 자주 보이는 것들이다

**지킬 것**

- 카메라는 정사영. 원근도 그림자도 없다
- 배경은 투명. 화면 색은 CSS 가 정한다
- `prefers-reduced-motion` 이면 아예 그리지 않는다
- 화면 밖으로 나가면 멈춘다. 배터리를 계속 먹으면 안 된다
- **WebGL 에 넘기는 배열은 컴포넌트 밖에 둔다.** 안에서 만들면 렌더마다 새 배열이라
  effect 가 캔버스를 통째로 다시 만든다 — 누르는 족족 날아간다

### 그림

**글만으로 화면을 채우지 않는다.** 특히 빈 화면은 글만 있으면 오류난 화면처럼 보인다.

에셋 명세는 `ASSET_PROMPTS.md` 에 있다. 규칙 두 가지.

- **한 장에 완성된 그림을 뽑지 않는다.** 부품(몸통 · 머리 · 표정 · 옷)을 따로 뽑아 겹친다.
  그래야 구성원이 늘어도 다시 뽑지 않는다
- 배경은 투명. 굵은 남색 외곽선. 그라데이션 · 그림자 없음

파일은 `public/assets/<분류>/<이름>.png` 에 둔다.
`Illustration` 컴포넌트는 파일이 없으면 조용히 숨으므로, 에셋이 오기 전에도 화면이 깨지지 않는다.

## 협업 규칙

### 브랜치

| 브랜치      | 역할                                                         |
| ----------- | ------------------------------------------------------------ |
| `main`      | 항상 배포 가능한 상태. 운영 반영·태그 관리                   |
| `develop`   | 다음 릴리스를 위한 통합 브랜치. 기능 브랜치가 모이는 곳      |
| `feature/*` | 단일 기능 단위. 완료 후 PR 로 develop 에 병합                |
| `hotfix/*`  | main 에서 발견된 긴급 이슈. 수정 후 main + develop 양쪽 반영 |
| `release/*` | 배포 전 최종 점검과 버전 태깅                                |

**작업 하나당 브랜치 하나다.** "프론트엔드" 같은 큰 브랜치는 만들지 않는다.

GitHub Ruleset 정규식 — `.husky/check-branch-name.mjs` 가 push 전에 같은 걸 검사한다.

```
^(feature|hotfix|chore|docs|release)/[\w\-]+
```

`feat/` 이 아니라 `feature/` 다. 슬래시는 한 번만 쓴다.

```
feature/FE-12-login-page
chore/FE-3-update-dependencies
hotfix/FE-9-fix-null-crash
release/v1-0-0
```

### 커밋

**작게, 자주 한다.** 기능 하나 만들면 커밋, 버그 고치면 커밋, 수정 하나 있어도 커밋.
여러 작업을 모아 한 번에 커밋하지 않는다. 브랜치 하나 안에 작은 커밋 여러 개가 쌓이고,
그게 PR 하나가 된다.

GitHub Ruleset 정규식 — `.husky/check-commit-msg.mjs` 가 커밋할 때 같은 걸 검사한다.

```
^(build|chore|ci|docs|feat|fix|perf|refactor|revert|style|test)(\([\w\p{Hangul}\-\.]+\))?(!)?: [\w\p{Hangul} ]+([\s\S]*)
```

무엇을 했는지가 제목에 있어야 한다.

| 나쁨              | 좋음                                    |
| ----------------- | --------------------------------------- |
| `feat: 로그인`    | `feat: 카카오 로그인 버튼 추가`         |
| `fix: 버그 수정`  | `fix: 측정 폼 선택 항목 검증 누락 수정` |
| `chore: 업데이트` | `chore(deps): TanStack Query 버전 올림` |

### 작업 순서

1. 이슈를 먼저 연다 (`[FEAT]` / `[BUG]` 템플릿)
2. 이슈 번호를 넣어 브랜치를 판다 — `feature/FE-<이슈번호>-<설명>`
3. 작은 단위로 커밋한다. `npm run verify` 가 통과해야 한다
4. develop 으로 PR 을 연다. PR 템플릿의 `Closes #<이슈번호>` 를 채운다
5. CI 통과를 확인하고 병합한다

> GitHub 는 **기본 브랜치(main)** 로 병합될 때만 `Closes #` 를 자동 처리한다.
> develop 으로 병합하는 우리 플로우에서는 이슈가 자동으로 닫히지 않으니 직접 닫는다.

### 커밋 전 확인

```bash
npm run verify     # format:check + lint + typecheck. CI 와 같은 검사
```
