# 화면 스펙 — 우리가족 체력키움

> 지금까지 받은 요구사항을 한 곳에 모은 것이다. **이 문서가 목표고, 화면은 이걸 따라간다.**
> 만들다 판단이 갈리면 여기를 고치고 나서 코드를 고친다.
>
> 규칙(하지 말 것·색·도메인)은 `AGENTS.md` 에 있다. 여기는 **무엇을 만들 것인가**다.

---

## 0. 아직 못 지킨 것

솔직하게 적는다. 아래가 이 문서를 쓰는 이유다.

| 요구                      | 지금      | 목표                                      |
| ------------------------- | --------- | ----------------------------------------- |
| 아이가 할 게 많아야 한다  | 화면 5개  | 매일 열 이유가 있는 화면                  |
| 눈으로 보기 재밌어야 한다 | 거의 없음 | 움직이고 반응하는 자리 넷                 |
| 부모 통계를 한눈에        | 표 하나   | 전적 사이트 수준의 밀도                   |
| 영상 추천이 라벨을 쓴다   | 안 씀     | 요인·강도·공간·소음·장비로 고른다         |
| 챗봇이 미션을 만든다      | 됨        | 더 깊게 — 아이 정보로 답하고 여러 개 제안 |
| 그림이 화면을 채운다      | 부분      | 브랜드 마크 · 머리 띠 · 구획 장식         |

---

## 1. 누가 무엇을 하는가

```
로그인 ─→ 역할 고르기 ─┬─→ 아이 홈
                       └─→ 부모 홈
```

역할은 **계정 권한과 별개의 보기 모드**다. 부모 계정이 로그인된 기기를 아이가 쓴다.

### 아이가 하는 일

| 무엇          | 어디서         | 매일 열 이유                     |
| ------------- | -------------- | -------------------------------- |
| 오늘 할 운동  | 홈 → 운동하기  | 하루에 하나, 끝내면 알린다       |
| 내 점수 보기  | 홈             | 또래 100명 속 내 자리가 움직인다 |
| 캐릭터 꾸미기 | 홈 → 내 캐릭터 | 운동한 날에 고를 게 늘어난다     |
| 받은 칭찬     | 홈 → 칭찬      | 부모가 보낸 한마디               |
| 이번 주 달력  | 홈             | 움직인 날이 칠해진다             |

### 부모가 하는 일

| 무엇           | 어디서                          |
| -------------- | ------------------------------- |
| 아이 상태 보기 | 홈                              |
| 오늘 알림에 답 | 홈 → 칭찬 보내기                |
| 한 주 짜기     | 홈 → 이번 주 제안               |
| 코치에게 묻기  | **어느 화면에서든 오른쪽 아래** |
| 기록 파고들기  | 홈 → 아이 기록                  |
| 가족 관리      | 홈 → 우리 가족                  |

---

## 2. 아이 홈 — 매일 열 이유

**순서가 곧 우선순위다.** 위에서부터 내려 읽으면 오늘 할 일이 보인다.

```
1. 인사 + 내 캐릭터        ← 고른 모습이 여기 나온다
2. 또래 100명 속 내 자리   ← three.js. 점수가 오르면 점이 움직인다
3. 이번 주 달력            ← 움직인 날만 칠한다. 연속 기록은 세지 않는다
4. 오늘 할 운동 (하나)     ← 가장 큰 누름 자리
5. 내가 모은 것            ← 본 영상 · 칭찬 · 꾸미기
```

### 지킬 것

- **누를 것이 둘을 넘지 않는다** — 4번이 주(主), 나머지는 아래로
- 등급·등수·연속 기록을 보여주지 않는다
- 숫자 0 을 나열하지 않는다. 아직 없으면 "이제 시작이에요"
- 코치 창을 띄우지 않는다

### 눈으로 보기 재밌는 자리

| 어디          | 무엇                                            |
| ------------- | ----------------------------------------------- |
| 또래 점 100개 | 들어올 때 한 번 흩어져 자리를 잡는다 (three.js) |
| 이번 주 달력  | 오늘 칸이 살짝 크고, 칠해질 때 채워 오른다      |
| 운동 시작     | 카드를 누르면 캐릭터가 한 번 튄다               |
| 다 했어요     | 조각이 터진다 (three.js)                        |
| 캐릭터 꾸미기 | 고르는 즉시 위 캐릭터가 바뀐다                  |

---

## 3. 부모 홈 — 한 화면에 다섯 덩어리까지

조사한 바로 한 화면의 항목이 대여섯을 넘으면 어느 것도 눈에 안 들어온다.

```
1. 아이 요약 스트립     ← 아바타 · 이름 · 점수 · 또래 대비 · 마지막 측정
2. 오늘                 ← 아이가 보낸 알림 → 칭찬 보내기
3. 이번 주              ← 코치 제안 상태 한 줄
4. 들어가는 곳 둘       ← 아이 기록 · 우리 가족
5. 나도 함께            ← 부모 자신의 측정
```

아이가 여럿이면 1번 위에 전환 줄을 둔다.

---

## 4. 아이 기록 — 전적 사이트의 밀도로

op.gg · dak.gg · maple.gg 가 공통으로 쓰는 배치를 따른다.

```
요약 스트립        누구 · 지금 몇 · 기준 대비 · 마지막 측정일
─────────────────
요인별 5줄          이름 · 막대(또래 평균 눈금) · 숫자
─────────────────
항목별 표           항목명 · 원값+단위 · 상위 N%
─────────────────
지금 몸             키 · 몸무게 · 잰 날
─────────────────
최근 기록           날짜 · 무엇 · 얼마나 · 무엇으로 확인됐는지
─────────────────
동작                다시 재기 · 측정 결과 · 10년 뒤
```

### 전적 사이트에서 가져오는 것

| 패턴                    | 우리 화면에서                          |
| ----------------------- | -------------------------------------- |
| 요약 카드 + 큰 숫자     | 신체 점수                              |
| 표 안의 가로 막대       | 요인별                                 |
| 오른쪽 정렬 고정폭 숫자 | 모든 수치                              |
| 기록 줄의 왼쪽 색 띠    | 최근 기록 — 확인된 기록과 직접 적은 것 |
| 기준선(평균) 눈금       | 또래 평균 50                           |

### 하지 않는 것

- 승/패처럼 초록·빨강으로 칠하지 않는다 (규칙 8)
- 등수를 매기지 않는다

---

## 5. 영상 추천 — 라벨을 실제로 쓴다

백엔드 `exercise_videos` 가 가진 라벨을 화면이 그대로 쓴다.

| 칼럼                | 화면에서                                             |
| ------------------- | ---------------------------------------------------- |
| `factors`           | 어느 체력 요인을 키우는지                            |
| `intensity`         | 가볍게 · 보통 · 세게                                 |
| `space`             | 좁은 공간 OK                                         |
| `noise`             | 조용함                                               |
| `equipment`         | 준비물 없음 / 무엇이 필요한지                        |
| `age_from`/`age_to` | 연령 안전 필터 — 라벨 없는 영상은 아이에게 안 나간다 |

### 오늘 할 운동을 고르는 규칙

1. 승인된 미션이 있으면 그 영상
2. 없으면 **가장 약한 요인**(`weakest.factor`)을 키우는 영상 중에서
3. 그중 **조용하고 좁은 공간에서 되는 것**을 먼저 (집에서 하는 서비스다)
4. 이미 완주한 영상은 뒤로

### 운동 고르기 화면

- 요인 칩으로 거른다 — 서버 `?factor=` 를 그대로 쓴다
- 각 카드에 라벨 배지를 붙인다
- 이미 본 영상은 진행률 막대를 보여준다

---

## 6. 코치 창 — 어느 화면에서든

오른쪽 아래에 떠 있고, 눌러서 연다.

```
질문 ─→ 답변 + 인용 근거 ─→ (미션 제안 카드) ─→ 미션 만들기
```

- 답변에 인용이 없으면 버그로 본다 (규칙 6)
- 제안 카드의 값이 그대로 미션 생성 본문이다
- 아이 화면에는 띄우지 않는다

### 백엔드에 요청한 것

`POST /coach/chat` 응답에 `suggestion?` 을 붙여 주면 카드가 뜬다. 없으면 답변만 보여준다.

---

## 7. 그림이 화면을 채운다

| 자리         | 무엇                   | 상태                |
| ------------ | ---------------------- | ------------------- |
| 브랜드 마크  | 로그인 · 상단 · 아이콘 | **없음** — 요청함   |
| 화면 머리 띠 | 화면마다 다른 장면     | 10장 중 4장         |
| 구획 장식    | 구역 사이              | 없음 — 요청함       |
| 캐릭터       | 동작마다 한 장         | 8장 (눈으로 확인함) |
| 아바타 부품  | 머리 · 표정 · 몸       | 겹치기 되는 것만 씀 |
| 기념 표시    | 해낸 것                | 없음 — 요청함       |

에셋 명세는 `ASSET_PROMPTS.md`(캐릭터) 와 `ASSET_PROMPTS_DECOR.md`(꾸미기) 에 있다.

---

## 8. 무엇으로 확인했는지 항상 적는다

서버가 진짜로 아는 것은 둘뿐이다.

| 근거             | 표기                         |
| ---------------- | ---------------------------- |
| `VIDEO_PROGRESS` | 영상 완주로 확인됨           |
| `TIMER`          | 타이머로 확인됨              |
| `SELF_REPORT`    | 직접 입력함 · 부모 확인 필요 |

놀이에서 센 횟수는 서버로 보내지 않는다. 시간만 보낸다.

---

## 9. 검사

| 명령                    | 무엇을 보나                                       |
| ----------------------- | ------------------------------------------------- |
| `npm run verify`        | 서식 · 린트 · 타입 · 에셋 참조 · 도메인 규칙      |
| `npm run check:screens` | 화면마다 오류 · 제목 · 누름 자리 · 아이 모드 누수 |
| `npm run check:clicks`  | 버튼과 링크를 하나씩 눌러 본다                    |
| `npm run check:down`    | 백엔드를 막고 사실대로 말하는지                   |
| `npm run clean:smudge`  | 그림에 남은 글자 자국                             |
