# 화면 스펙 v3 — 우리가족 체력키움

> **이 문서가 목표고, 화면은 이걸 따라간다.** 만들다 판단이 갈리면 여기를 고치고 나서 코드를 고친다.
>
> 근거는 둘이다. 흐름 시연판(v2)과 **9월 23일 회의**(「운동 영상 기반 AI 추천 시스템 설계 회의」).
> 규칙(하지 말 것·색·도메인)은 `AGENTS.md`, 백엔드에 막힌 것은 `BACKEND_ASKS.md`,
> 그림 주문은 `ASSET_PROMPTS.md` 에 있다.

---

## 0. v2 에서 무엇이 바뀌나

| 축                 | v2                           | v3                                                    |
| ------------------ | ---------------------------- | ----------------------------------------------------- |
| **보는 결**        | 흰 바탕에 선으로 구획        | **삼성헬스 · 애플 피트니스** — 회색 바탕 위 흰 카드   |
| **아이 통계**      | 항목 목록 · 분포 구름        | **체력 육각형** 하나. 맨 위에                         |
| **추천을 받는 법** | 코치 대화창 + 칩             | **버튼만.** 대화창은 없앤다                           |
| **영상의 단위**    | 한 편 속 구간 (0:12–2:10)    | **2분 안팎 클립 여러 개**가 순서대로 온다             |
| **재는 법**        | 영상 90% 완주                | **시간.** 영상은 동작 시범, 타이머가 따로 돈다        |
| **하는 화면**      | 세션 하나씩 들어가서         | **한 화면에 아래로 이어진다.** 끝나면 스스로 내려감   |
| **계속하는 힘**    | 기운 항아리 · 리그 · 쉬는 날 | **레벨 · 경험치 · 업적 · 연속** (리그·쉬는 날은 뒤로) |
| **칭찬**           | 정해진 문구                  | **칭찬 스티커** + 메모(선택)                          |
| **기록을 보는 곳** | 지난 기록 목록               | **캘린더.** 부모 · 아이 둘 다                         |
| **알리는 법**      | 없음                         | **알림** — 설정 옆 종 아이콘                          |

---

## 1. 회의에서 정한 것 (프론트에 걸리는 것만)

| 정한 것                                                         | 화면에 어떻게                                   |
| --------------------------------------------------------------- | ----------------------------------------------- |
| 부모는 그래프 **하나**로 "우리 아이가 이 정도다" 를 보고 싶다   | 부모 홈 맨 위 = 육각형 + 신체 점수              |
| 지식 그래프처럼 생긴 분포 그림은 "전혀 모를 것 같다"            | `PeerCloud` 삭제                                |
| 영상은 60초 안팎 클립, 준비 2 · 본 2 · 정리 2 처럼 한 번에 온다 | 받은 순서 그대로 세로로 잇는다                  |
| **시간으로 잰다.** 영상은 방법을 보여 주고 몇 분 할지는 따로    | 칸마다 타이머. 영상이 짧으면 타이머 동안 반복   |
| 한 미션에 영상이 10개를 넘으면 짜증난다                         | 10개 넘게 오면 앞의 10개만, 나머지는 "다음에"   |
| 채팅이 아니라 **버튼으로 요청**                                 | `/plan` 조건 칩                                 |
| 요일 · 시간대를 받는다. 추천 내용은 시간으로 정해지지 않는다    | `/settings/schedule` — 편성의 기본값으로만 쓴다 |
| 레벨 · 경험치 계산은 **서버**, 꾸미는 건 프론트                 | `GET /profiles/{id}/progress` 요청, 목으로 먼저 |
| 캐릭터 꾸미기는 뺀다, 프레임 애니메이션도 뺀다                  | `/kid/me` · 캐릭터 조립 · 동작 프레임 삭제      |
| 칭찬은 스티커. 메모는 선택. 캘린더에 붙는다. 칭찬하면 경험치    | 4.4 · 6.3                                       |
| 아이 키 · 몸무게는 금방 변한다 — 앱 안에서 알린다               | 알림 + 홈 카드. **한 달** 기준                  |
| 운동 목록을 검색 · 카테고리로 보고 **직접 루틴을 짠다**         | `/videos` 에서 담기 → 오늘 운동으로             |
| 즐겨찾기 — 재밌었던 걸 다시 하고 싶다                           | 영상 카드의 하트                                |
| 가족 정보 수정 화면이 필요하다                                  | `/parent/family` 에서 고친다                    |
| 헤더가 화면 1/3 을 차지하면 안 된다                             | 위 막대는 44px, 홈만 큰 제목                    |
| 제출은 **구현 이미지**다                                        | 화면마다 스크린샷 한 장으로 설명되게            |

**보류** — 리그(한 달 단위), 쉬는 날 카드. 회의에서 넣자고 했지만 남의 가족 값이 있어야
성립한다. 레벨 · 업적이 먼저고, 리그는 서버가 순위를 줄 때 붙인다.

---

## 2. 보는 결

### 2.1 면

```
바탕   --color-ground  #F4F5F7   화면 전체
카드   --color-paper   #FFFFFF   내용은 전부 카드 위에
눌림   --color-sub     #F1F3F6   카드 안의 칸
```

- **카드마다 무게가 다르다.** 화면의 주인공 하나(`card-hero`)만 크고, 나머지는 작다.
  크기 · 역할이 같은 카드를 줄줄이 세우면 그게 AI 가 만든 화면이다
- 카드 제목은 작고 굵게(`metric-label`), 숫자는 크게(`metric-value`), 단위는 작게
- 카드 오른쪽 위 `›` 는 "눌러서 더 본다" 뜻이다. 누를 수 없는 카드에는 달지 않는다

### 2.2 서체

**숫자도 본문 서체(Pretendard)다.** 둥근 숫자 서체(Fredoka)를 걷어낸다 —
화면마다 서체가 두 개면 그게 "중구난방" 의 한 원인이다.
큰 숫자는 비례 숫자, 표 안 숫자만 고정폭(`tabular-nums`).

### 2.3 그래프

| 그래프     | 어디                    | 모양                                                                   |
| ---------- | ----------------------- | ---------------------------------------------------------------------- |
| 또래 바다  | 부모 홈 · 아이 자세히   | 요인 기둥 한 줄 — 높이 = 또래 백분위, 물 높이 = 또래 평균 50 (입체)    |
| 육각형     | 같은 자리 — 고르면      | 여섯 꼭지점 고정. 아이 = 파랑 선 + 옅은 면, 또래 = 회색 선 (평면)      |
| 이번 주 탑 | 부모 홈 · 아이 홈       | 요일마다 육각 기둥 — 높이 = 그날 분, 오늘 노랑, 쉰 날 납작한 판 (입체) |
| 링         | 오늘 운동               | 시간 · 운동 · 이번 주 세 겹. 애플 피트니스의 링                        |
| 선         | 아이 자세히 — 점수 추이 | 50 기준선을 같이                                                       |
| 키 자      | 아이 자세히 — 키        | 잴 때마다 눈금 하나, 키움이가 마지막 키만큼 옆에 선다 (입체)           |
| 징검다리   | 운동하기 · 짜는 중      | 칸마다 돌 하나, 끝나면 다음 돌로 건넌다 (입체)                         |

입체는 전부 키움 섬의 결이다(정사영 · 두 톤 · 남색 외곽선). **값을 속이지 않는다** —
높이는 값에 곧게 비례하고, 숫자는 글자로 얹고, 기준(평균 고리)을 같이 그린다(AGENTS.md).
모든 그래프는 표로 한 번 더 읽힌다(육각형 아래 여섯 줄 등). 색만으로 구분하지 않는다.

---

## 3. 화면 지도

역할이 곧 내비게이션이다. **하단 탭바는 만들지 않는다.** 홈이 카드 판이고, 카드가 길이다.

```
온보딩 (그대로)
  /  /login  /claim  /start  /start/family  /start/child  /settings/support-mode

부모
  /parent                  홈 — 육각형 · 오늘 운동 · 이번 주 · 레벨 · 운동 찾기
  /parent/child/[id]       아이 자세히 — 육각형 크게 · 요인 표 · 점수 추이 · 키/몸무게
  /parent/update/[id]      신체정보 다시 재기
  /parent/family           가족 관리 · 초대 · 정보 고치기
  /parent/sticker/[id]     칭찬 스티커 붙이기                      ＋
  /p/[id]/measure|result|future   측정 · 결과 · 10년 뒤

AI 편성
  /plan                    조건 고르기                             ＋
  /plan/run/[runId]        짜는 과정                               ＋
  /plan/[runId]            제안 · 근거 · 세션 → 오늘 운동으로 등록  ＋
  /plan/custom             직접 짜기 — 담은 동작 · 누가 · 언제(여러 날 · 몇 주) ＋

아이
  /kid                     홈 — 캐릭터 · 레벨 · 오늘 운동 · 이번 주
  /kid/m/[id]              운동하기 — 타이머 + 영상, 아래로 이어짐
  /kid/badges              레벨과 업적 · 레벨마다 열리는 것          ＋
  /kid/play                놀이터 — 레벨이 오르면 열리는 몸 놀이     ＋
  /kid/play/dice|freeze|follow   운동 주사위 · 얼음땡 · 따라 해 봐    ＋

같이
  /calendar                캘린더 — 부모 · 아이 둘 다              ＋
  /notifications           알림                                    ＋
  /videos                  운동 찾기 — 카테고리 · 검색 · 담기
  /settings                설정 — 누가 쓰는지 · 동의 · 로그아웃만
  /settings/schedule       운동할 수 있는 시간                     ＋
```

---

## 4. 부모

### 4.1 홈 `/parent`

위에서 아래로 읽으면 **아이가 어디쯤인지 → 오늘 무엇을 하는지 → 이번 주가 어땠는지** 순이다.

```
서준이네                               [종] [톱니]
9월 23일 화요일
(서준) (지우)                ← 아이가 둘 이상일 때만

┌ 서준의 체력 ────────────── 9월 7일 측정 › ┐   card-hero
│            [ 육각형 ]                      │
│  신체 점수 62   또래 평균 50               │
│  ● 서준  ─ 또래 평균                       │
│  (서버 headline 그대로)                    │
└────────────────────────────────────────────┘
┌ 키 · 몸무게를 새로 잴 때예요 ──────────── › ┐   한 달이 지났을 때만
┌ 오늘 운동 ───────────────── (링 8/12분) ── ┐
│ 6개 · 12분 · 저녁 7시                      │
│ 준비 2 · 본 2 · 정리 2                     │
│ [상태] 시작 전 / 3개 했어요 / 다 했어요    │
│        → 다 했으면 [칭찬 스티커 붙이기]    │
└────────────────────────────────────────────┘
┌ 이번 주 ─────── › ┐ ┌ 레벨 ─────────── › ┐   반쪽 카드 둘
│ 42분 · 3일       │ │ Lv.4  (캐릭터)      │
│ ▂▅▁▇▁▁▁          │ │ ▰▰▰▱▱ 120/250       │
└──────────────────┘ └─────────────────────┘
┌ 키우고 싶은 힘으로 찾기 ─────────────────── ┐
│ (심폐)(근력)(근지구)(유연성)(민첩)(순발)     │
└────────────────────────────────────────────┘
┌ 가족 ─────────────────────────────────── › ┐
```

- 오늘 운동이 없으면 그 카드는 **[AI 에게 오늘 운동 짜 달라기]** 가 된다 → `/plan`
- 측정이 없으면 육각형 자리는 빈 육각형 + "첫 측정을 등록하면 지도가 그려져요"(서버 문구)

### 4.2 아이 자세히 `/parent/child/[id]`

```
또래 바다 (입체 · 육각형 고름) + 신체 점수
요인 표 — 여섯 줄: 요인 · 한 마디 설명 · 백분위 막대(50 눈금) · band 문구
            안 잰 요인은 "아직 안 쟀어요"
점수 추이 — 측정 회차별 선, 50 기준선
키 · 몸무게 — 회차별 키 자(잴 때마다 눈금). 한 달이 지났으면 [다시 재기]
레벨 · 업적 요약 → /kid/badges?profile=
```

### 4.3 칭찬 스티커 `/parent/sticker/[profileId]?missionId=`

**고르기만 해도 보내진다.** 퇴근한 부모에게 글쓰기를 시키면 그날로 안 보낸다(규칙 12).

```
오늘 서준이는 12분 움직였어요 (무엇을 했는지 한 줄)
스티커 12종 격자 — 누르면 고른 것 하나가 커진다
메모 (선택) — 한 줄, 60자
[붙이기]
```

- 보낸 스티커는 그날 캘린더에 붙고 아이에게 알림이 간다
- 스티커 개수를 목표로 만들지 않는다. "10개 모으면" 없음

---

## 5. AI 편성

### 5.1 조건 `/plan`

**채팅이 아니라 칩이다.** 열린 질문을 받으면 답하면 안 되는 질문까지 들어온다.

| 무엇    | 고르는 것                | 기본값                    |
| ------- | ------------------------ | ------------------------- |
| 누가    | 아이 혼자 · 부모와 같이  | 참여 방식                 |
| 몇 분   | 10 · 20 · 30 · 40        | **오늘의 운동 가능 시간** |
| 어디서  | 집 · 밖                  | 집                        |
| 소리    | 조용히 · 상관없어요      | 조용히                    |
| 키울 힘 | 알아서(추천) · 여섯 요인 | 알아서 = 가장 낮은 요인   |

### 5.2 짜는 과정 `/plan/run/[runId]`

스피너 하나로 때우지 않는다. **무엇을 보고 있는지** 한 줄씩 남는다(`CoachRun.steps`).
위에는 징검다리 — 단계마다 떠 있는 돌 하나, 한 단계를 마치면 키움이가 다음 돌로 건너간다.
계속 도는 것은 지금 단계의 고리 하나다.

```
측정 기록 읽기     서준 · 유소년 · 3항목
키울 힘 찾기       유연성 — 또래보다 낮아요
또래 처방 찾기     국민체력100 처방 12건
영상 고르기        클립 500개 중 조건에 맞는 18개
순서 짜기          준비 2 · 본 2 · 정리 2 · 12분
```

### 5.3 제안 `/plan/[runId]`

```
[제안 · 아직 등록 전]                    ← 항상 맨 위
유연성 키우기 12분                        (서버 title)
근거 — rationale + [근거 1][근거 2]      (인용 카드는 펼침)
준비운동  ▢ 팔 벌려 뛰기   1분
          ▢ 어깨 돌리기    1분
본운동    ▢ ...            3분
정리운동  ▢ ...            1분
[오늘 운동으로 등록]
[다시 짜기]  [이번엔 안 할래요]          ← 거절은 사유를 받는다
```

- 등록 전에는 "미션" 이라고 쓰지 않는다(규칙 1). 등록하면 아이에게 알림
- 등록 버튼은 부모 프로필에만 있다

### 5.4 직접 짜기 `/plan/custom`

9/23 요청 — "선택해서 미션을 생성한다거나". AI 편성의 다른 길이다. 부모가 고른 것이라
제안을 거치지 않고 바로 그날의 운동이 된다(삼성헬스 · 애플 피트니스의 「운동 만들기」 · 프로그램).

```
운동 찾기 — 키우고 싶은 힘으로 골라 [담기] … ┌ 담은 동작 4개 · 10분   [짜러 가기 ›] ┐

직접 짜기
┌ 동작 4개 · 10분            준비 → 본 → 정리로 ┐   card-hero
│ ① 거북이 스트레칭 · 본운동                  ×  │
│    (− 3분 +)                          (↑)(↓) │
│ …                                            │
│              동작 더 담기                     │
└──────────────────────────────────────────────┘
누가 할까요   (은영) [서준] (도현)              가족 여럿 — 엄마랑 같이
언제 할까요   [오늘] 목 [금] 토 [일] 월 화       운동할 수 있는 날에 점 · 바꾸기
              되풀이  이번 한 번 · 2주 · 4주
┌ 4개 · 10분 · 서준                           ┐
│ [6일에 등록]                                  │   오늘 하나면 「오늘 운동으로 등록」
└──────────────────────────────────────────────┘
```

- 동작은 열 개까지. 처음 담을 때 본운동 3분 · 준비 · 정리 1분, 1~5분으로 바꾼다
- **날마다 따로 등록한다** — 한 날을 못 해도 다른 날은 그대로 남는다
- 여러 날이면 등록한 뒤 캘린더로 — 앞으로의 날에 점선 고리로 보인다
- 들어오는 길: 부모 홈 오늘 운동이 비었을 때 · AI 편성 첫 화면 「직접 고를래요」 · 운동 찾기 쟁반
- 짜는 중인 루틴은 탭에만 남는다(등록 전이라 서버가 모른다). 등록하면 비운다

---

## 6. 아이

### 6.1 홈 `/kid`

```
                                   [종]
      ╱‾‾‾‾‾‾‾‾‾╲    키움 섬 (three.js)
     │ 나무 (레벨  │    운동한 날마다 나무 하나
      ╲  캐릭터) ╱     손으로 돌리고, 누르면 캐릭터가 뛴다 · 떠다니지 않는다
        서준 · Lv.4 · 나무 14그루   ▰▰▰▱▱ 다음 레벨까지 80
┌ 오늘 운동 ─────────────────────────────┐   card-hero, 파랑
│ 6개 · 12분                             │
│ [시작하기]                              │
└────────────────────────────────────────┘
오늘 움직인 만큼  ◎ 시간 · 운동 · 이번 주   ← 부모 홈과 같은 링
▮ ▮ ▫ ▫ ▫ ▫ ▫  요일 기둥 탑   3일 이어서 했어요
받은 스티커 (최근 셋)            캘린더 ›
업적 (최근 셋)                   전부 ›
```

- **키움 섬** — 떠 있는 육각형 섬 가운데에 레벨 캐릭터가 선다. 운동한 날마다 나무가
  하나씩 자라고 **줄지 않는다.** 2D 캐릭터를 종이 인형처럼 세운 입체다(AGENTS.md)
- **해야 할 일은 하나다** — 큰 버튼 하나. 나머지는 보는 곳으로 가는 길
- 다 했으면 "오늘 거 다 했어요". 다음 영상을 권하지 않는다
- 등급 · 약한 요인 · 형제 비교는 여기 없다(규칙 10)

### 6.2 운동하기 `/kid/m/[id]` — 길라잡이

**한 화면에 아래로 이어진다.** 왼쪽 세로선이 길이고, 칸이 정거장이다.

```
⬢ ⬢ (키움이)⬢ ⬡ ⬡ ⬡⚑  2/6                ← 위에 붙는 징검다리
 ✓ 준비운동  팔 벌려 뛰기          1:00
 │
 ◉ 준비운동  어깨 돌리기           1:00     ← 지금 칸만 펼친다
 │ ┌ 영상 (시범) ─────────────────┐
 │ │                              │
 │ └──────────────────────────────┘
 │      (  0:43  )  [멈춤]
 │
 ○ 본운동    스쿼트                2:00
 ○ ...
[다 했어요]  ← 마지막 칸이 끝나면
```

- **시작을 누르면 타이머와 영상이 같이 돈다.** 영상이 타이머보다 짧으면 구간을 되풀이한다
- 한 칸이 끝나면 조각이 한 번 터지고, 위 징검다리에서 키움이가 다음 돌로 건너간다.
  3초 뒤 **화면이 다음 칸으로 스스로 내려간다**
- 지금 칸의 영상만 띄운다. 나머지는 썸네일 — 영상 여섯 개를 한꺼번에 띄우면 폰이 버벅인다
- 브라우저가 자동 재생을 막으면 타이머를 멈추고 "눌러서 시작" 을 띄운다
- 중간에 [여기까지 할래요] 로 끝내도 한 만큼 남는다
- 기록은 `TIMER` 로 남는다 — 우리가 잰 시간이다(규칙 2)

### 6.3 마침

```
(키움 섬 — 오늘 나무가 자라난다 · 조각 · 레벨이 오르면 새 장식이 튀어나온다)
오늘 거 다 했어요!   12분
+40 경험치           ▰▰▰▰▱ Lv.4
새로 열렸어요 · 울타리 — 섬에 울타리가 섰어요
[엄마한테 알리기]    ← 누르면 부모에게 알림
```

부모가 스티커를 보내기 전에는 "엄마가 보고 있어요". 재촉하지 않는다.
레벨이 올라 놀이가 열렸으면 「얼음땡 하러 가기」 — 놀이터로 잇는다.

### 6.4 레벨과 업적 `/kid/badges`

```
Lv.4 · 320 경험치 · 다음 레벨까지 130
캐릭터 다섯 단계 (지금 단계만 진하게, 앞으로 될 모습은 흐리게)
레벨마다 열리는 것 — 연 것 체크 · 다음 하나 「다음이에요」 · 그 뒤는 무엇이 생기는지만
업적 격자 — 얻은 것은 색, 아직인 것은 회색 + 어떻게 얻는지
  └ 얻은 것을 누르면 육각 금메달이 한 바퀴 반 돌며 나타남 (three.js · 손으로 돌려 봄)
최근에 얻은 경험치 — 서버가 준 숫자 그대로
경험치는 이렇게 얻어요 — 숫자 없이. 규칙은 서버 것이다 · 줄지 않는다
```

### 6.5 놀이터 `/kid/play` — 레벨이 오를수록 넓어진다

9/23 요청 — "레벨이 커질수록 앱 자체가 재밌어지게, 해금되는 기능 · 앱 안에서 하는 재밌는 활동 · 게임".
레벨마다 하나씩, 섬 꾸미기와 놀이가 번갈아 열린다(`lib/unlocks.ts`).

| Lv  | 열리는 것          | Lv  | 열리는 것 |
| --- | ------------------ | --- | --------- |
| 1   | 놀이 · 운동 주사위 | 6   | 섬 · 연못 |
| 2   | 섬 · 깃발          | 7   | 섬 · 텐트 |
| 3   | 놀이 · 얼음땡      | 8   | 섬 · 풍차 |
| 4   | 섬 · 울타리        | 9   | 섬 · 등대 |
| 5   | 놀이 · 따라 해 봐  |     |           |

```
┌ 몸으로 하는 놀이 ──────────────┐   가장 최근에 열린 놀이 하나 — card-hero
│ 따라 해 봐                     │
│ [시작하기]                     │
└───────────────────────────────┘
운동 주사위 · 얼음땡 · 따라 해 봐    아직인 것은 「Lv.N이 되면 열려요」
Lv.6이 되면 섬에 연못이 생겨요
```

| 놀이        | 하는 것                                                                    | 입체                                      |
| ----------- | -------------------------------------------------------------------------- | ----------------------------------------- |
| 운동 주사위 | 굴려서 윗면에 나온 동작을 한다                                             | 섬 판 위 주사위 — 튀어 오르며 구른다      |
| 얼음땡      | 박자에 맞춰 움직이다 「얼음!」 이면 멈춘다. 5~10초마다, 여섯 번이면 한 판  | 키움이가 박자에 뛰다 얼음 기둥에 갇힌다   |
| 따라 해 봐  | 키움이가 해 보인 동작을 순서대로. 둘에서 시작해 한 판마다 하나씩, 여섯까지 | 키움이가 몸 전체로 흉내(만세 · 한 바퀴 …) |

- **놀이는 몸 놀이다.** 운동을 화면 시간으로 바꿔 주지 않는다 — 폰은 심판이고 몸이 논다
- 점수 · 순위 · 기록을 남기지 않는다. 따라 했는지는 옆에서 보는 가족이 본다
- 열리는 건 전부 덤이다. 오늘 운동 · 캘린더 · 칭찬 · 측정은 레벨로 막지 않는다
- 얼음땡 소리는 파일 없이 브라우저가 짓는다(Web Audio). 누른 순간에만 켜고, 끌 수 있다

---

## 7. 같이 보는 곳

### 7.1 캘린더 `/calendar`

```
(서준 ▾)                      ← 부모만. 아이는 자기 것
‹ 2026년 9월 ›
월 화 수 목 금 토 일
날짜 칸 — 숫자 · 한 날은 작은 링(잡힌 시간 대비) · 받은 스티커 하나
고른 날 —
  9월 22일 월 · 12분
  준비운동 팔 벌려 뛰기 1분 · 타이머로 확인
  ...
  (스티커) 엄마 · "끝까지 했네!"
  [칭찬 스티커 붙이기]        ← 부모 · 스티커 없는 날만
```

- 아무것도 안 한 날은 빈 칸이다. "빠진 날" 이라고 쓰지 않는다

### 7.2 알림 `/notifications`

| 누구에게 | 언제                    | 문구 예                              | 누르면        |
| -------- | ----------------------- | ------------------------------------ | ------------- |
| 부모     | 아이가 운동을 마쳤을 때 | 서준이가 오늘 운동을 마쳤어요 · 12분 | 스티커 붙이기 |
| 부모     | 측정 뒤 **한 달**       | 서준이 키 · 몸무게를 새로 재 볼까요  | 다시 재기     |
| 아이     | 스티커를 받았을 때      | 엄마가 스티커를 붙여 줬어요          | 캘린더 그날   |
| 아이     | 오늘 운동이 등록됐을 때 | 오늘 운동이 생겼어요 · 6개 12분      | 운동하기      |
| 아이     | 업적 · 레벨             | 새 업적 — 3일 이어서                 | 레벨과 업적   |

- 종 아이콘은 **설정 톱니 옆**. 안 읽은 게 있으면 점 하나(숫자 아님)
- 앱 밖 푸시는 설정에서 켠다. 꺼 둔 사람도 앱에 들어오면 여기서 본다

### 7.3 운동 찾기 `/videos`

```
[검색]
(전체)(심폐)(근력)(근지구)(유연성)(민첩)(순발)     ← 키우고 싶은 힘
(준비)(본)(정리)   (조용히)                        ← 거르기
영상 카드 — 썸네일 · 제목 · 1:05 · 요인 · [♡] [+ 담기]
─ 담은 운동 3개 · 4분  [오늘 운동으로 만들기] ─   ← 아래에 붙는 쟁반
```

- 담은 운동은 준비 → 본 → 정리 순으로 세워서 미션을 만든다(`POST /missions`, `origin: MANUAL`)
- 라벨 없는 영상은 아이에게 내보내지 않는다

### 7.4 운동할 수 있는 시간 `/settings/schedule`

```
(서준)(은영)                     ← 사람마다
월 [켬]  저녁 7:00  20분
화 [끔]
...
빠른 선택  (평일 저녁) (주말 오전)
```

편성의 **기본값**으로만 쓴다. 이 시간이 아니라고 운동을 막지 않는다.

---

## 8. 계약에 없는 것 — 백엔드에 요청

자세한 모양은 `BACKEND_ASKS.md`. 올 때까지는 목 서버가 같은 모양으로 답한다.

| 무엇                 | 요청                                                                       |
| -------------------- | -------------------------------------------------------------------------- |
| 레벨 · 경험치 · 업적 | `GET /profiles/{id}/progress` (섬의 나무 수 `activeDays` 포함)             |
| 운동 가능 시간       | `GET · PUT /profiles/{id}/availability`                                    |
| 캘린더               | `GET /families/{id}/calendar?profileId=&month=`                            |
| 알림                 | `GET /notifications?profileId=` · `POST /notifications/read`               |
| 칭찬 스티커          | `CheerRequest.stickerId` (지금은 `emoji` 칸에 싣는다)                      |
| 편성 조건            | `StartCoachRunRequest` 에 `place` · `noise` · `focusFactor` · `withParent` |
| 클립 순서            | `MissionView.sessions[]` (v2 요청 그대로) + 칸별 `minutes`                 |
| 직접 짠 미션         | `CreateMissionRequest.sessions[]`                                          |

---

## 9. 만드는 순서

**보여주기 → 실행하기 → 지속하기.** 단계마다 시연 가능한 상태로 남긴다.

| 브랜치                        | 이슈 | 무엇                                                      |
| ----------------------------- | ---- | --------------------------------------------------------- |
| `feature/FE-29-health-look`   | #29  | 토큰 · 위 막대 · 육각형 · 부모 홈 · 아이 자세히 · 아이 홈 |
| `feature/FE-30-plan-and-play` | #30  | 운동 가능 시간 · AI 편성 · 운동하기 · 운동 찾기           |
| `feature/FE-31-keep-going`    | #31  | 레벨 · 업적 · 스티커 · 캘린더 · 알림                      |
| `feature/FE-34-kium-island`   | #34  | three.js 키움 섬 — 아이 홈 · 마침 · 로그인 · 아이 자세히  |
| `feature/FE-38-art-slots`     | #38  | 그림 자리 정리 — 주문한 그림만, 오기 전엔 비워 둔다       |
| `feature/FE-40-webgl-scenes`  | #40  | 입체 그래프 · 키 자 · 징검다리 — 섬의 결로                |

---

## 10. 버리는 것

| 무엇                            | 왜                                  |
| ------------------------------- | ----------------------------------- |
| 코치 대화창                     | 추천은 버튼으로 받는다 (회의)       |
| `PeerCloud` 분포 구름           | 알아보기 어렵다 (회의)              |
| `/kid/me` 캐릭터 꾸미기         | 뺀다 (회의). 레벨 캐릭터가 대신한다 |
| 캐릭터 조립 그림 · 동작 프레임  | 이어지는 그림이 안 나온다 (회의)    |
| `/parent/history`               | 캘린더가 대신한다                   |
| `/family/report`                | 홈의 이번 주 카드 + 캘린더          |
| `/family/cheer` · `/kid/praise` | 칭찬 스티커 · 캘린더                |
| `/kid/done`                     | 운동하기 끝 화면 + 캘린더           |
| `/coach/weekly`                 | `/plan/*`                           |
| `/kid/m/[id]/swap`              | 다시 짜기는 부모가 `/plan` 에서     |
| 기운 항아리                     | 레벨 · 경험치로 합친다              |
| Fredoka 숫자 서체               | 서체는 하나                         |

---

## 10-2. 참고한 앱 — 가져온 것 · 가져오지 않은 것

9/23 요청 — "삼성헬스 · 애플 피트니스 · 그 외 홈트 앱 · 헬스 앱을 참조해서 좋은 기능을 뽑아 적용".
조사한 것 중 **아이와 부모가 같이 쓰는 이 서비스의 결에 맞는 것**만 가져왔다.

| 가져온 곳                                 | 기능                       | 이 앱에서                                                                                                         |
| ----------------------------------------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| 애플 피트니스 활동 링                     | 세 겹 링                   | 오늘 움직인 만큼 — 시간 · 운동 · 이번 주 (부모 · 아이 홈)                                                         |
| 애플 피트니스 달력                        | 날마다 작은 링             | 캘린더 — 그날 잡힌 시간 대비 움직인 시간                                                                          |
| 삼성헬스 프로그램 · 애플 「운동 만들기」  | 직접 짜서 여러 날에        | 직접 짜기 — 동작 · 누가 · 언제(여러 날 · 몇 주)                                                                   |
| 나이키 트레이닝 클럽 · 삼성헬스 운동 코칭 | 소리 안내                  | 운동하기 — 시작 · 10초 남음 · 셋 둘 하나 · 다음은                                                                 |
| 애플 피트니스+ · 펠로톤                   | 쉬는 시간 조절 · 운동 강도 | 10초 쉬기 · +10초 · 바로 시작, 다 했어요 「어땠어요?」                                                            |
| 애플 피트니스 공유 · 삼성헬스 투게더      | 같이 채우는 목표           | 우리 가족 다리 — **가족 합만**, 누가 몇 분인지 나누지 않는다                                                      |
| 애플 피트니스 공유                        | 주고받기                   | 아이가 받은 스티커에 「고마워요」 를 돌려보낸다                                                                   |
| 삼성헬스 주간 리포트                      | 한 주 돌아보기             | 지난주 돌아보기 — 해낸 것만(움직인 날 · 분 · 가장 많이 한 날 · 스티커 · 업적). 닫으면 다음 주까지                 |
| 삼성헬스 홈 편집                          | 홈 고치기                  | 부모 홈 맨 아래 「홈 편집」 — 지난주 돌아보기 · 이번 주 · 가족 다리 · 찾기 · 가족을 숨긴다. 체력 · 오늘 운동은 늘 |
| 게임 앱의 레벨                            | 레벨마다 열리는 것         | 섬 꾸미기 · 놀이터 — 덤만 연다(규칙 13)                                                                           |

**가져오지 않은 것**

| 기능                                    | 까닭                                             |
| --------------------------------------- | ------------------------------------------------ |
| 친구 · 가족 순위표, 경쟁(애플 「대결」) | 형제 비교가 된다(규칙 10)                        |
| 끊기면 0 으로 돌아가는 연속 기록        | 끊긴 날이 벌이 된다. 며칠 이어서 했는지만 적는다 |
| 움직이면 게임 시간을 버는 앱            | 운동을 화면 시간으로 바꾼다. 놀이터는 몸 놀이다  |
| 쉬는 날 슬퍼하는 캐릭터 · 시드는 식물   | 벌이 된다. 나무는 줄지 않는다                    |
| 아이 화면의 몸무게 · BMI                | 아이가 자기 몸을 점수로 본다                     |
| 칼로리 · 지방 연소                      | 아이에게 맞지 않는 말이다                        |

---

## 11. 바뀌지 않는 것

1. **제안은 미션이 아니다.** 부모가 등록해야 미션이 된다
2. **무엇으로 확인했는지 적는다.** 자기 신고에 「자동 인증」 이라고 쓰지 않는다
3. **10년 뒤는 예언이 아니다.** p10~p90 음영과 고지를 항상 같이
4. **등급을 경고색으로 칠하지 않는다.** 육각형도 한 색이다
5. **서버가 준 문구를 고쳐 쓰지 않는다.** 신체 점수는 서버의 `overallPercentile` 이다 —
   육각형 여섯 값을 프론트가 평균 내서 점수라고 부르지 않는다
6. **칭찬은 부모가 보낸다**
7. 하단 탭바 없음 · 이모지 없음 · 다크 모드 없음 · 국민체력100 색
