# 화면 스펙 — 우리가족 체력키움

> **이 문서가 목표고, 화면은 이걸 따라간다.** 만들다 판단이 갈리면 여기를 고치고 나서 코드를 고친다.
>
> 근거는 흐름 시연판(v2)이다. 23개 화면을 눌러 보면서 정한 것을 여기에 옮겼다.
> 규칙(하지 말 것·색·도메인)은 `AGENTS.md`, 백엔드에 막힌 것은 `BACKEND_ASKS.md` 에 있다.

---

## 0. 무엇이 달라지는가

지금 앱은 **한 번에 하나씩** 하는 앱이다 — 영상 하나 보고, 미션 하나 하고, 칭찬 하나 받는다.
새 흐름은 **한 주를 짜고 하루를 세 조각으로 나눈다.** 바뀌는 축이 넷이다.

| 축              | 지금                  | 여기서부터                                    |
| --------------- | --------------------- | --------------------------------------------- |
| **미션의 단위** | 영상 한 편            | **준비 · 본 · 정리 세 세션**                  |
| **영상의 단위** | 영상 한 편 (90% 완주) | **영상 속 구간** (0:12–2:10 만)               |
| **바꾸는 법**   | 목록에서 다른 걸 고름 | **왜 바꾸는지 받고** 같은 요인 안에서 골라 옴 |
| **계속하는 힘** | 칭찬 한마디           | **가족 기운 항아리 · 리그 · 쉬는 날 카드**    |

그리고 **지금 막혀 있는 것 하나** — 새 사용자는 온보딩 첫 관문을 못 넘는다(3절).

---

## 1. 어디까지 왔나

| 요구                          | 지금                                                               |
| ----------------------------- | ------------------------------------------------------------------ |
| 새 사용자가 끝까지 간다       | **막힘** — `POST /families` · `/profiles` · `/auth/google` 목 없음 |
| 미션이 세 세션이다            | **없음** — 계약에 `phase` 자체가 없다                              |
| 영상을 구간으로 본다          | **반쪽** — `startSec` 만 있고 `endSec` 가 없다                     |
| 구간을 끝내면 기록된다        | **틀림** — 영상 **전체** 90%를 봐야 완주로 친다                    |
| 바꿀 때 이유를 받는다         | **없음** — 연령대 목록을 그냥 보여 준다                            |
| 코치가 무엇을 보는지 보인다   | **없음** — 단계는 있지만 근거 문장이 없다                          |
| 조건을 바꿔 다시 짠다         | **없음**                                                           |
| 가족 기운 · 리그 · 쉬는 날    | **없음**                                                           |
| 영상 안을 챕터로 펼친다       | **없음**                                                           |
| 그날 하루를 들여다본다        | **없음**                                                           |
| 참여 방식을 처음에 고른다     | 설정 안쪽에만 있다                                                 |
| 아이도 부모에게 응원을 보낸다 | 「같이 하자고 하기」 하나뿐                                        |

---

## 2. 화면 지도 — 23개

역할이 곧 내비게이션이라는 원칙은 그대로다. **하단 탭바를 만들지 않는다.**

```
온보딩
  /                      시작 — 토큰 보고 갈 곳을 정한다
  /login                 로그인
  /claim                 초대코드 ▸ 자리 하나에 맞는 코드
  /start/family          가족 만들기
  /start/child           아이 추가 · 보호자 동의
  /start/support         참여 방식 고르기             ＋온보딩으로 올림

부모
  /parent                가족 대시보드                ＋전면 개편
  /parent/child/[id]     아이 상세
  /parent/day/[date]     그날 하루                    ＋새로
  /parent/library        운동 찾아 담기               ＋새로
  /parent/family         가족 관리 · 초대
  /p/[id]/measure        측정
  /p/[id]/result         측정 결과
  /p/[id]/future         10년 뒤

AI 편성
  /coach/run             코치가 짜는 과정             ＋새로
  /coach/weekly          제안 · 근거 · 세션
  /coach/retune          조건 바꿔 다시 짜기          ＋새로

아이
  /kid                   아이 홈
  /kid/m/[id]            준비 · 본 · 정리             ＋새로
  /kid/m/[id]/[n]        n번째 세션 하기 (구간 영상)  ＋세션 단위로
  /kid/m/[id]/swap       다른 운동 받기               ＋이유를 먼저
  /kid/cheer             응원 보내기                  ＋아이도 보낸다

보상
  /family/jar            가족 기운 항아리             ＋새로
  /family/league         가족 리그                    ＋새로
  /family/rest           쉬는 날 카드                 ＋새로
  /family/report         주간 기록
  /settings              설정
```

`＋` 가 지금 없는 화면이다. **열넷.**

---

## 3. 온보딩 — 막힌 곳부터

### 3.1 지금 무엇이 막혀 있나

화면은 부르는데 목 서버가 답하지 않는 주소가 셋이다.

| 주소                | 부르는 곳   | 목  |
| ------------------- | ----------- | --- |
| `POST /auth/google` | 로그인      | ✗   |
| `POST /families`    | 가족 만들기 | ✗   |
| `POST /profiles`    | 아이 추가   | ✗   |

요청이 브라우저를 빠져나가 `localhost:8080` 으로 나가고, 백엔드가 없으니 실패한다.
**시연 계정(`demo-parent`)으로만 앱이 돌아간다** — 처음 쓰는 사람의 경로가 한 번도 안 돌아 본 것이다.

먼저 이 셋을 목에 넣는다. 만들어진 가족·프로필은 `sessionStorage` 에 남겨 새로고침을 견딘다.

### 3.2 초대코드는 **자리 하나**에 맞는다

가족 전체가 쓰는 코드가 아니다. `GET /profiles/{profileId}/invite` 가 이미 프로필 단위라 계약은 맞다 —
**화면이 그렇게 말하지 않고 있을 뿐이다.**

- 코드를 넣으면 「서준이네 · **아빠 자리**」 가 뜬다. 받는 사람은 역할을 고를 수 없다
- 아이가 그 코드를 받아도 **아이 자리**로만 들어온다
- 왜 이렇게 두는가 — 부모 권한이 곧 코치 승인 권한이다. 받은 사람이 자기를 부모라고 우기는 길을 막는다
- 헷갈리는 `0 O 1 I` 는 코드에서 뺀다. 7일 만료

### 3.3 참여 방식을 온보딩으로 올린다

지금은 설정 안쪽에 있어서 **처음 쓰는 부모는 자기가 무엇을 고른 건지 모른 채 시작한다.**
가족 만들기 → 아이 추가 **→ 참여 방식** → 대시보드.

세 칸에는 **고르면 아이 화면이 어떻게 바뀌는지**를 같이 적는다. 이게 이 화면의 전부다.

| 고른 것      | 코치 편성               | 아이 화면에 뜨는 말       |
| ------------ | ----------------------- | ------------------------- |
| `CHEER_ONLY` | 내 미션 없음            | 「엄마가 보고 있어요」    |
| `WEEKEND`    | 주말 미션에만 나를 넣음 | 「토요일에 같이 나가요」  |
| `FULL`       | 나도 측정하고 내 미션도 | 「엄마도 오늘 같이 해요」 |

---

## 4. 부모

### 4.1 가족 대시보드 `/parent`

위에서부터 읽으면 **이번 주가 어떻게 가고 있는지**가 보인다.

```
1. 연속 불꽃           ← 며칠 이어서 했나. 끊겨도 깎지 않는다
2. 이번 주 달력        ← 날짜를 누르면 그날로 (4.2)
3. 가족이 움직인 시간  ← 하루별 막대 하나짜리 그래프
4. 리그 배지           ← 누르면 /family/league
5. 기운 항아리         ← 누르면 /family/jar
6. 이번 주 제안        ← 승인 전에만. 누르면 /coach/run
7. 구성원              ← 아이 · 나 · 아직 안 들어온 사람
8. 응원 보내기
9. 운동 찾아 담기      ← /parent/library
```

전에 여기 있던 「아이 기록」·「가족 관리」 카드 둘은 그대로 없다 — 가는 곳이 이미 화면 안에 있다.

### 4.2 그날 하루 `/parent/day/[date]` ＋

달력의 날짜를 누르면 열린다. **빈 날과 쉬는 날을 가른다.**

| 그날            | 화면                                                              |
| --------------- | ----------------------------------------------------------------- |
| 한 날           | 몇 분 · **무엇으로 확인했는지** · 누구와 · 한 것 목록 · 받은 응원 |
| 쉬기로 한 날    | 「쉬었어요 — 쉬는 날도 기록이에요」                               |
| 아무 일 없던 날 | 같은 화면. **「빠진 날」 이라고 쓰지 않는다**                     |

### 4.3 운동 찾아 담기 `/parent/library` ＋

**영상이 강의고, 구간이 챕터다.**

지금은 영상 목록만 있고 영상 **안**을 볼 수 없어서, 부모가 루틴을 직접 짤 방법이 없다.

```
검색 ─ 요인 칩(전체·유연성·심폐지구력·근력·근지구력)
 └ 강의 한 편 (제목 · 길이 · 연령 · 라벨)
     └ 챕터 (준비/본/정리 · 동작 이름 · 0:12–2:10 · 요인) [담기]
담은 것 n개 → 미션 만들기 (준비·본·정리 순으로 정렬)
```

`/videos` 는 여기로 흡수한다. **아이에게는 영상 목록을 주지 않는다** — 아이가 할 일은 오늘 것 하나다.

### 4.4 나도 재기 `/p/[id]/measure`

부모가 잰 날, 아이 홈에 「엄마도 쟀어요」 가 뜬다.
**시키는 사람이 아니라 같이 하는 사람이 되는 지점이다.** 화면은 그대로 두고 이 연결만 잇는다.

---

## 5. AI 편성

### 5.1 코치가 짜는 과정 `/coach/run` ＋

스피너 하나로 때우지 않는다. **무엇을 보고 있는지** 단계마다 한 줄씩 남긴다.

| 단계                       | 남기는 말 (예)                                                       |
| -------------------------- | -------------------------------------------------------------------- |
| 가족과 최근 측정을 읽는 중 | 서준 유소년 남 8세 · 측정 3항목 · 은영 성인 여 측정 없음             |
| 약한 곳을 찾는 중          | 유연성이 가장 낮아요(상위 76%). 75% 아래라 「키우기」로 잡아요       |
| 또래 운동처방을 찾는 중    | 국민체력100 유소년-8-M 처방 12건 · 준비 4 · 본 5 · 정리 3            |
| 영상 구간을 고르는 중      | 운동 5개 → 구간 9개. 조용함 · 좁은 공간 · 준비물 없음으로 4개 걸러냄 |
| 한 주를 짜는 중            | 일간 5건 · 주간 1건 · 회당 15분 · 부모도 같이 2회                    |

나중에 「왜 이 운동이지?」 를 되짚을 수 있어야 한다. 이 줄들은 `CoachRun.steps` 에 남는다.

### 5.2 제안 `/coach/weekly`

```
승인 전 · 미션 0건        ← 이 배지는 항상 맨 위
이번 주 배치 7칸          ← 🏠 집에서 3번 · 🏞 밖에서 1번 · 쉬는 날 3일
 ├ 집에서 하는 것          요일 칩 · 왜 이 운동인지 + 근거 인용 · 세션 3줄
 └ 밖에서 하는 것          같은 모양
[승인하고 아이에게 보내기] [조건 바꿔 다시] [이번 주는 쉴래요]
```

세션 한 줄은 **구간 썸네일 + 동작 이름 + 준비/본/정리 + 요인 + 분**이다.
영상이 없는 세션은 썸네일 자리에 「영상 없음」 이라고 적는다 —
**라벨 없는 영상은 아이에게 내보내지 않는다**(도메인 규칙).

### 5.3 조건 바꿔 다시 `/coach/retune` ＋

**채팅이 아니라 칩이다.** 여섯 줄.

| 무엇          | 고르는 것                  |
| ------------- | -------------------------- |
| 한 번에 몇 분 | 5 · 15 · 30                |
| 소음          | 조용해야 해요 · 상관없어요 |
| 공간          | 좁아요 · 넓어요            |
| 어디서        | 집에서 · 밖에서 · 둘 다    |
| 부모는        | 같이 해요 · 응원만         |
| 세기          | 쉽게 · 보통 · 좀 힘들게    |

왜 칩인가 — 열린 질문을 받으면 「우리 애 살 빼려면?」 같은 **답하면 안 되는 질문**까지 들어온다.
고를 수 있는 것만 두면 막을 것이 없고, 부모는 자기가 조종하고 있다고 느낀다.

> **코치 창(`ChatDock`)은 남긴다. 역할을 나눈다.**
> 편성을 **바꾸는** 길은 칩(`retune`), **묻는** 길은 대화(`ChatDock`)다.
> 대화는 인용 근거가 붙고 거부 응답이 있어서(`refused`) 열린 질문이 그대로 통과하지 않는다.
> 둘 중 하나를 버려야 한다면 버리는 쪽은 대화다 — 편성을 바꾸는 건 칩으로 충분하다.

---

## 6. 아이

### 6.1 아이 홈 `/kid`

```
1. 인사 + 받은 응원        ← 부모가 보낸 한마디가 맨 위
2. 연속 불꽃
3. 이번 주 달력
4. 얼마나 움직였나         ← 막대 그래프
5. 오늘 같이 하는 사람 셋  ← 나 · 엄마(참여 방식대로) · 아빠
6. 기운 항아리 · 리그 배지
7. 오늘 할 운동 큰 카드    ← [시작하기]
   └ [다른 거 하고 싶어] [오늘은 쉴래요]
```

**누를 것이 둘을 넘지 않는다** — 7번이 주(主)고 나머지는 보는 것이다.

- 오늘 다 했으면 7번이 「오늘 거 다 했어요」 로 바뀐다. **다음 영상을 권하지 않는다**
- 쉬기로 했으면 「오늘은 쉬는 날 — 쉬는 것도 하는 일이에요」
- 어제 피드백을 남겼으면 맨 아래에 「어제 『어려웠어』 라고 한 거, 코치가 반영했어요」

### 6.2 미션 = 세션 셋 `/kid/m/[id]` ＋

```
준비운동   팔 벌려 뛰기                   2분   [0:12–2:10]
본운동     다리 벌려 앞으로 상체 숙이기   10분   [1:36–3:06]
정리운동   나비자세                       3분   [9:40–12:18]

[준비운동 시작]  [이거 말고 다른 거]
```

- **하나만 해도 기록에 남는다.** 다 못 채워도 괜찮다고 적는다
- 셋 다 끝나면 [다 했어요!]

### 6.3 구간 영상 `/kid/m/[id]/[n]` ＋

**영상 전체가 아니라 구간만 재생한다.**

- 플레이어는 `startSec` 에서 시작해 `endSec` 에서 멈춘다
- 완주 판정은 **구간 길이의 90%** 다. 영상 전체 90%가 아니다
- 진행 막대는 구간 기준. 「0:12 ─ 62% ─ 2:10」
- 중간에 그만둬도 [여기까지 할래요] 로 남는다

> 지금 앱은 영상 전체 90%를 완주로 친다. 90초짜리 구간만 하면 **영원히 완주가 안 된다.**
> 구간의 시작과 끝을 알아야 고칠 수 있다 — 8절.

영상이 없는 세션(「공원까지 걷고 오기」)은 타이머나 세기 놀이로 간다.

### 6.4 다른 운동 받기 `/kid/m/[id]/swap` ＋

**목록을 보여 주지 않는다. 이유를 먼저 받는다.**

```
왜 바꾸고 싶어요?
 [너무 힘들어요] [쿵쿵거려서 안 돼요] [시간이 없어요] [재미없어요]
        ↓
그럼 이건 어때요?  ← 그 이유에 맞는 대안 둘
  같은 유연성 안에서 골랐어요. 방향은 그대로 두고 동작만 바꿉니다 [근거 2]
  고른 이유는 이번 주 편성에도 남아요
```

| 이유          | 어떻게 고르나                        |
| ------------- | ------------------------------------ |
| 너무 힘들어요 | 같은 요인 · `intensity` 한 단계 아래 |
| 쿵쿵거려서    | 같은 요인 · `noise = QUIET`          |
| 시간이 없어요 | 같은 요인 · 구간 길이 절반 이하      |
| 재미없어요    | 같은 요인 · **아직 안 해 본 것**     |

`/kid/pick` 은 이 화면으로 대체한다.

### 6.5 마침 — `/kid/m/[id]` 의 끝 ＋

```
🎉 오늘 거 다 했어요!        엄마에게 바로 알려 줬어요
우리 가족 이번 주 100%       엄마도 오늘 같이 했어요

오늘 어땠어?
 [재밌었어] [어려웠어] [다른 게 좋아]
   → 고마워요. 다음 주 편성에 이걸 넣을게요

[엄마에게 고맙다고 하기]  [홈으로]
기운 340 · 오늘 한 만큼 항아리에 들어갔어요
```

피드백 세 칸이 **다음 주 편성에 들어간다.** 아이가 남긴 말이 다음 주를 바꾸는 유일한 자리다.

### 6.6 응원 보내기 `/kid/cheer` ＋

**아이도 부모에게 보낸다.** 방향이 한쪽이면 응원이 아니라 감시다.

```
누구에게 보낼까요?   [서준에게] [엄마에게] [아빠에게]
무슨 말을           [오늘도 잘했어!] [같이 해서 좋았어] [네가 자랑스러워]
                    [내일도 화이팅] [푹 쉬어] [다음엔 아빠도 같이 하자]
```

도메인 규칙 12(「칭찬은 부모가 보낸다」)와 충돌하지 않는다 —
**칭찬**은 여전히 부모가 보내고, 아이가 보내는 건 **고맙다·같이 하자**다.
화면에서도 말을 나눈다: 부모가 보내면 「칭찬」, 아이가 보내면 「응원」.

---

## 7. 보상 — 출석이 아니라 누적

### 7.1 가족 기운 항아리 `/family/jar` ＋

**누가 하든 항아리 하나에 모인다.** 형제끼리 누가 많이 했는지는 세지 않는다.

| 쌓이는 것           | 기운 | 왜 그만큼                   |
| ------------------- | ---- | --------------------------- |
| 영상 구간을 끝까지  | +20  | 서버가 확인한 것            |
| 타이머 10분         | +15  | 서버가 확인한 것            |
| 걸음수 직접 입력    | +5   | **사람이 말한 값이라 절반** |
| 처음 해 보는 운동   | +10  | 같은 것만 반복하지 않게     |
| 가족이 같은 날 같이 | +15  | 혼자 할 때보다 크게         |

- **빠진 날에 깎지 않는다.** 한 번 쌓인 기운은 줄지 않는다
- 목표는 **그 가족 지난 4주 평균에 맞춰 코치가 잡는다.** 조금만 더 하면 닿는 선
- 다 차면 **부모가 건 약속**이 이뤄진다. 앱이 주는 게 아니다

기운은 **활동 누적**이지 신체 점수가 아니다. 도메인 규칙 10(「신체 점수는 우리가 지어낸 수가 아니다」)과
섞이지 않게 화면에서 **「기운」과 「신체 점수」를 절대 같은 줄에 두지 않는다.**

### 7.2 가족 리그 `/family/league` ＋

**체력이 아니라 목표 달성률로 겨룬다.**

- 목표는 집집마다 다르다(7.1). 그래서 운동 잘하는 집이 유리하지 않고, 식구가 많다고 유리하지도 않다
- 이름은 **가족 단위로만** 나온다. 집 안에서 누가 더 했는지는 어디에도 안 나온다
- 티어 여섯: 브론즈 · 실버 · 골드 · 플래티넘 · 다이아 · 마스터. 일요일 밤에 정리
- 위 다섯 집 승급, 아래 네 집 강등
- **두 가족이 안 모이면 화면을 잠근다.** 가짜 가족을 채워 넣지 않는다 — 혼자 1등 하는 순위표는 뜻이 없다

### 7.3 쉬는 날 카드 `/family/rest` ＋

**미리 누르면 빈 날이 아니다.**

| 카드를 쓰면 | 그날                        |
| ----------- | --------------------------- |
| 달력        | 빈 칸 대신 「쉬는 날」      |
| 리그        | 그 주에는 강등 없음         |
| 알림        | 아무것도 안 보낸다          |
| 기운        | 쌓인 건 그대로              |
| 연속        | **끊기지 않고 이어 붙는다** |

한 달에 두 장. 학원 있는 날, 시험 기간, 아픈 날에 쓰라고 있는 것이다.

---

## 8. 계약에 없는 것 — 백엔드에 요청

이 흐름은 **계약에 없는 개념 셋** 위에 서 있다. 요청이 `BACKEND_ASKS.md` 로 간다.

### 8.1 영상 구간 — `endSec`

지금 `startSec` 만 있다. 구간의 끝을 모르면 구간 재생도 구간 완주도 안 된다.

```
VideoView.chapters?: [{ startSec, endSec, title, factor, phase }]
ProposalVideoView.endSec?: number
POST /videos/{id}/progress  ← clipStartSec · clipEndSec 를 같이 받아 구간 기준으로 판정
```

**올 때까지** — 프론트가 구간 시간을 재서 보낸다. 다만 그건 **서버가 본 게 아니라 우리가 잰 것**이라
`verifiedBy` 를 `VIDEO_PROGRESS` 가 아니라 `TIMER` 로 적는다. 도메인 규칙 2를 어기지 않는다.

### 8.2 세션 — `phase`

```
MissionView.sessions?: [{ position, phase: "WARMUP"|"MAIN"|"COOLDOWN",
                          title, factor, minutes, video?, completed, verifiedBy }]
ProposalView.sessions?: 같은 모양
CreateMissionRequest.sessions?: 같은 모양 (라이브러리에서 담아 만든 미션)
POST /missions/{id}/sessions/{n}/done
```

**올 때까지** — 세션이 안 오면 **본운동 한 칸만** 그린다. 준비·정리를 지어내지 않는다.

### 8.3 기운 · 리그 · 쉬는 날

| 무엇      | 지금 할 수 있는 것                                      | 요청                                   |
| --------- | ------------------------------------------------------- | -------------------------------------- |
| 기운      | **프론트가 계산한다** — 미션·verifiedBy·응원에서 나온다 | `GET /families/{id}/points` (나중)     |
| 리그      | 할 수 없다. 남의 가족 값이 필요하다                     | `GET /leagues/current`                 |
| 쉬는 날   | 기기에 둔다 (달력 · 알림만 바뀜)                        | `POST /families/{id}/rest-days`        |
| 피드백    | 보낼 곳이 없다                                          | `POST /missions/{id}/feedback`         |
| 주간 배치 | 제안에 요일이 없다                                      | `ProposalView.days?: ["월","수","금"]` |

리그는 **잠김 화면부터 만든다.** 「두 가족이 모이면 그때부터 겨뤄요」 가 거짓이 아니라 사실이다.

### 8.4 온보딩 세 주소

`POST /auth/google` · `POST /families` · `POST /profiles` — **목부터 채운다**(3.1).
실제 백엔드는 이미 있거나 곧 온다. 목이 없어서 프론트가 못 도는 것뿐이다.

---

## 9. 도메인 규칙 개정안 — `AGENTS.md` 10

연속과 리그를 넣기로 했으므로 규칙 10을 고친다. **조건과 함께 고쳐야 앞뒤가 맞는다.**

```diff
  #### 아이 화면에서 보여주는 것과 아닌 것
- | 보여준다            | 보여주지 않는다        |
- | 신체 점수 하나      | 등급(1·2·3·참가)       |
- | 이번 주에 움직인 날 | 약한 항목              |
- | 오늘 할 운동 하나   | 형제·가족 구성원 비교  |
- | 받은 도장과 받은 말 | 연속 기록이 끊겼다는 말 |
+ | 보여준다                | 보여주지 않는다           |
+ | 신체 점수 하나          | 등급(1·2·3·참가)          |
+ | 이번 주에 움직인 날     | 약한 항목                 |
+ | 오늘 할 운동 하나       | **형제·가족 구성원 비교** |
+ | 받은 도장과 받은 말     | 개인 등수                 |
+ | **며칠 이어서 했는가**  |                           |
+ | **가족 단위 리그 순위** |                           |
+
+ **연속은 보여주되 깎지 않는다.** 끊겨도 잃는 것이 없고, 쉬는 날 카드로 이어 붙일 수 있다.
+ 「연속이 끊겼습니다」 라고 쓰지 않는다 — 며칠 이어서 했는지만 적는다.
+
+ **순위는 가족 단위로만.** 겨루는 값은 체력이 아니라 그 가족의 목표 달성률이고,
+ 목표는 집집마다 지난 4주에 맞춰 따로 잡는다. 집 안에서 누가 더 했는지는 어디에도 나오지 않는다 —
+ 그게 나오는 순간 형제끼리 겨루게 된다. **밖으로는 겨루고 안으로는 같이 채운다.**
```

이 개정은 `AGENTS.md` 를 실제로 고칠 때 한 번 더 확인받고 넣는다.

---

## 10. 만드는 순서

**막힌 것 → 구간·세션 → 보상.** 매 단계 끝마다 시연 가능한 상태가 유지된다.

### 1단계 — 온보딩이 끝까지 간다

- [ ] `POST /auth/google` · `POST /families` · `POST /profiles` 목 핸들러 + 탭 저장소
- [ ] `/start/family` → `/start/child` → `/start/support` → `/parent` 로 잇기
- [ ] 참여 방식을 온보딩으로 올리고 「아이 화면에 이렇게 떠요」 미리보기 추가
- [ ] 초대코드를 **자리 코드**로 — 화면 문구와 받는 쪽 역할 고정
- [ ] 끝까지 걸어 보는 검사 추가 (`check:onboarding`)

### 2단계 — 미션이 세 세션, 영상이 구간

- [ ] `MissionSession` · `VideoClip` 타입 + 목 (세션 없으면 본운동 한 칸)
- [ ] `YouTubePlayer` 에 `endSec` · 구간 완주 판정 · 구간 진행 막대
- [ ] `/kid/m/[id]` 준비·본·정리, `/kid/m/[id]/[n]` 세션 하기
- [ ] `/kid/m/[id]/swap` 이유 받고 같은 요인 안에서
- [ ] 마침 화면 피드백 칩 셋
- [ ] `/coach/run` 과정 노출, `/coach/weekly` 주간 배치 + 세션, `/coach/retune` 칩
- [ ] `/kid/pick` 삭제

### 3단계 — 보상과 나머지

- [ ] `/family/jar` 기운 항아리 (프론트 계산)
- [ ] `/family/league` 잠김 상태부터
- [ ] `/family/rest` 쉬는 날 카드
- [ ] `/parent/library` 강의·챕터 · 담아서 미션 만들기
- [ ] `/parent/day/[date]` 그날 하루
- [ ] `/kid/cheer` 아이가 보내는 응원
- [ ] 대시보드·아이 홈에 불꽃·그래프·항아리·리그 얹기

---

## 11. 버리는 것

| 무엇                      | 왜                                                  |
| ------------------------- | --------------------------------------------------- |
| `/kid/pick`               | `/kid/m/[id]/swap` 이 대신한다 — 이유를 먼저 받는다 |
| 아이 화면의 영상 목록     | 아이가 할 일은 오늘 것 하나다                       |
| `/videos` 세 탭           | `/parent/library` 로 흡수                           |
| 영상 전체 90% 완주 판정   | 구간 기준으로 바꾼다                                |
| 「다른 운동 고르기」 목록 | 추천이 아니라 나열이었다                            |

---

## 12. 바뀌지 않는 것

흐름이 바뀌어도 이건 그대로다. `AGENTS.md` 에 있고 여기서 다시 확인한다.

1. **제안은 미션이 아니다.** 보호자가 승인해야 미션이 된다. 승인 전 미션 0건
2. **무엇으로 확인했는지 항상 적는다.** 자기 신고에 「자동 인증」 이라고 쓰지 않는다
3. **10년 뒤는 예언이 아니다.** p10~p90 음영과 고지를 항상 같이 그린다
4. **등급을 경고색으로 칠하지 않는다**
5. **서버가 준 문구를 고쳐 쓰지 않는다**
6. **코치 답변에는 근거가 붙는다**
7. 하단 탭바 없음 · 이모지 없음 · 흰 배경 하나 · 국민체력100 색
